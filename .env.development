# Development: API calls go through Vite's dev proxy (→ Flask on 3001)
# This is empty intentionally — the Vite proxy handles /api routing locally.
# VITE_API_BASE_URL is intentionally left unset so the app uses relative paths.
VITE_API_BASE_URL=
